import styled from '@emotion/styled';

interface FollowBtnProps {
  label: string;
  Follower: number;
}

const Follow = styled.button<FollowBtnProps>``;

const FollowBtn = ({ label, Follower }: FollowBtnProps) => {
  return <></>;
};
